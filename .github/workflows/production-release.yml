name: Production Release Workflow

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    uses: Battlegrounds-Development/build-logic/.github/workflows/production-release.yml@main
